# OdysseyGen 部署环境变量模板（复制为 .env 后填写真实值）
# cp .env.example .env && vim .env

DB_USERNAME=root
DB_PASSWORD=你的数据库密码
# 生产环境务必使用 ≥32 位随机字符串；修改会使线上旧 JWT 全部失效
JWT_SECRET=请替换为至少32位随机字符串
DEEPSEEK_API_KEY=你的DeepSeek API Key
